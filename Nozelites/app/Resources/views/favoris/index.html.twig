{% extends 'base.html.twig' %}

{% block body %}
    <h1>Favoris list</h1>

    <table>
        <thead>
            <tr>
                <th>Idfav</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for favori in favoris %}
            <tr>
                <td><a href="{{ path('favoris_show', { 'idFav': favori.idFav }) }}">{{ favori.idFav }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('favoris_show', { 'idFav': favori.idFav }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('favoris_edit', { 'idFav': favori.idFav }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('favoris_new') }}">Create a new favori</a>
        </li>
    </ul>
{% endblock %}
