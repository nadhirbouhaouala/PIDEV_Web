{% extends 'base.html.twig' %}

{% block body %}
    <h1>Favori</h1>

    <table>
        <tbody>
            <tr>
                <th>Idfav</th>
                <td>{{ favori.idFav }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('favoris_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('favoris_edit', { 'idFav': favori.idFav }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
